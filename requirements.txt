# Combined dependencies for a fresh clone of this repo.
# Install from the repository root:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# Shared
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.0

# Diabetes prediction
scikit-learn>=1.3

# Zomato EDA (PDF report)
reportlab>=4.0.0
Pillow>=9.0.0
